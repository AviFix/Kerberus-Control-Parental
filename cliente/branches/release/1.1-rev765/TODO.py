# * Todas las URLs inválidas deberian estar permitidas
# * Ni bien se cambie la contraseña se deberia notificar.
# * utilizar HTTPS
# * hacer que se descargue desde el servidor la lista de servidores disponibles
# * La pagina de sitio denegado tiene que estar en el server, no en el cliente.
# * Hacer actualizador para linux.
# * Internacionalizacion
# * Hacer que thunderbird utilice el proxy(al menos en linux no se setea).
